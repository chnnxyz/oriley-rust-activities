// Rust uses fn for function, and the script must be inside main like in C
fn main() {
    println!("Hello, world!");
}

// Cargo run anywhere within the project folder runs the program.

// Cargo clean removes target/debug and its contents
